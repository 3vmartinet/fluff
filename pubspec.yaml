name: fluff
description: "Flutter Leveraging User Functions from Friends."
version: 0.0.1
homepage: https://github.com/3vmartinet/fluff

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  android_id: ^0.4.0
  device_info_plus: ^10.1.0
  flutter:
    sdk: flutter
  flutter_adaptive_scaffold: ^0.2.2
  intl: ^0.19.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

